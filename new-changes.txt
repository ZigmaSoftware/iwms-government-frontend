# Task: Port Schedule Masters to IWMS Government (Frontend + Backend) & Remove Company/Project ID

## Context
There are two IWMS systems in this workspace:
- **Source (reference implementation):** `/home/sathya/iwms/web/iwms-frontend` and `/home/sathya/iwms/web/iwms-backend`
- **Target (to implement in):** `/home/sathya/iwms/government/iwms-government-frontend` and `/home/sathya/iwms/government/iwms-government-backend`

The source system has a fully working **Schedule Masters** module. The government system needs the
same functionality, adapted to its corporation-based structure.

## Part 1 — Port Schedule Masters into the Government system

Study these 5 forms in the source system (frontend UI + backend models/serializers/views/URLs)
and re-implement them in the government frontend and backend:

1. **Trip Plans** (`scheduleMasters/tripPlan`, `app/serializers/schedule_masters/trip_plan_serializer.py`)
2. **Daily Trip Plan** (daily trip assignment/tracking — see `daily_trip_assignment_serializer.py`,
   `dailyTripTracking/`)
3. **Bin Collection Event** (`binCollectionEvent/`, `bin_collection_event_serializer.py`)
4. **Waste Collected Data** (`wasteManagementMasters/wasteCollectedData/`)
5. **Collection Points** (`masters/collectionPoint/`, `schedule_masters/collection_point_serializer.py`)

Also port the **auto-schedule logic** from the source backend:
- `app/services/daily_trip_scheduler.py`
- `app/services/daily_trip_generation.py`
- any related management commands / cron entries (`scheduler.sh`, `promote_draft_trip_logs.py`, etc.)

Placement in the government frontend:
- Master/setup forms (Trip Plans, Collection Points, and related config) go under a **"Schedule Setup"** section.
- Transactional/day-to-day forms (Daily Trip Plan, Bin Collection Event, Waste Collected Data) go under a
  **"Daily Operations"** section.
- Register routes in `AdminEncryptedRouter.tsx`, add sidebar entries in `AppSidebar.tsx`, and follow the
  government frontend's existing module structure (`src/pages/admin/modules/...`), form/table components,
  and API-hook conventions — match the government codebase's patterns, not the source's, where they differ.

Backend: create the corresponding Django models, serializers, views, URLs, and migrations in
`iwms-government-backend`, following its existing app structure and permission/middleware conventions.

## Part 2 — Remove Company ID and Project ID (corporation-based instead)

The government system is scoped **per corporation** via the Config form — company and project are
no longer needed:
- Remove `company` / `company_id` and `project` / `project_id` fields and filters from the government
  frontend and backend, starting with **Staff Creation** and the **Config form** (the Config form has
  already been reworked to be per-corporation — use it as the reference for how scoping should work).
- Apply the same corporation-based scoping to the newly ported Schedule Setup / Daily Operations
  modules: anywhere the source system filtered by company/project, the government version should scope
  by corporation from the config instead.
- Update backend models, serializers, querysets, and validations accordingly, with migrations for any
  removed/changed fields.

## Requirements
- Reuse the source system's business logic and UI as the blueprint, but adapt naming, auth, and scoping
  to the government system's conventions.
- Keep TypeScript types, i18n/locales, and encrypted routing consistent with existing government modules.
- Run migrations cleanly and verify each of the 5 forms end-to-end (list, create, edit, delete, and
  auto-schedule generation).
- Work through this module by module; after each module, confirm the frontend builds and backend checks pass.

dont ask me, i'll give you a full access to you so place that and i'll add upon fix the vehicle breakdown too which and all forms need to be in the government

continue
