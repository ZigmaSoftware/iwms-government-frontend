# Task: Trip Plan with Inline Collection Points (IWMS Government) & Remove the Separate Trip Plan Collection Point Form

## Context
There are two IWMS systems in this workspace:
- **Source (reference implementation):** `/home/sathya/iwms/web/iwms-frontend` and `/home/sathya/iwms/web/iwms-backend`
- **Target (to implement in):** `/home/sathya/iwms/government/iwms-government-frontend` and `/home/sathya/iwms/government/iwms-government-backend`

The Schedule Masters port into the government system is already done. This task reworks the
**Trip Plan** form and removes the standalone **Trip Plan Collection Point** form.

## Part 1 — Trip Plan form: use the source UI with inline Collection Points

Use the source system's Trip Plan form as the UI blueprint:
- Source form: `iwms-frontend/src/pages/admin/modules/transportMasters/tripPlan/tripPlanForm.tsx`
- Source serializer (nested stop writes): `iwms-backend/app/serializers/schedule_masters/trip_plan_serializer.py`
- Target form to update: `iwms-government-frontend/src/pages/admin/modules/transportMasters/tripPlan/tripPlanForm.tsx`

The government Trip Plan form must match that UI exactly (see the reference screenshot):

**Top section (two-column grid):**
- District, City, Zone, PLB (Participating Local Bodies) — cascading location dropdowns,
  scoped by corporation from the Config (government hierarchy, NOT company/project/panchayat/ward)
- Staff Template, Vehicle, Supervisor
- Waste Type (multi-select)
- Start Time (time picker), Trigger Weight (kg), Max Vehicle Capacity (kg)
- Status (Active/Inactive), Approval Status (Pending/Approved/Rejected)
- Keep the source validations: "Select either Zone or PLB", "Trigger weight must be less than
  vehicle capacity", required-field checks — adapted to government field names.

**Collection Points section (embedded card inside the same form, NOT a separate page):**
- Header: "Collection Points" with subtitle "Add all stops for this trip plan before saving."
  and an **Add Stop** button on the right.
- One row per stop with columns: **Seq** (auto, read-only), **Type** (Bin Collection /
  Household Collection dropdown), **Collection Point** (dropdown, filtered by the selected
  location and excluding points already used in other rows), **Bin** (dropdown scoped to the
  selected Collection Point and the plan's Waste Types), **Active** (toggle), **Remove** button
  (disabled when only one row remains).
- Stops are saved **with the trip plan in one request** as nested `collection_points`, exactly
  like the source `trip_plan_serializer.py` does (create/replace child rows on save, return
  them as `plan_collection_points` on read). Update the government
  `trip_plan_serializer.py` / views to support this nested write if they don't already.

## Part 2 — Remove the standalone Trip Plan Collection Point form

Since stops now live inside the Trip Plan form, remove the separate module:
- Delete the frontend module `iwms-government-frontend/src/pages/admin/modules/scheduleMasters/tripPlanCollectionPoint/`
  (form, list, types), its route in `AdminEncryptedRouter.tsx`, and its sidebar entry.
- Backend: keep the `trip_plan_collection_point` model/table (it stores the nested stops), but
  remove the standalone CRUD API for it (serializer-as-endpoint, views, URLs) if it exists —
  it must only be written through the Trip Plan nested serializer.
- Remove any i18n/locale keys, permissions entries, and TypeScript types that only existed for
  the standalone form.

## Part 3 — AppSidebar: use my fixed version, don't add extras

I have already fixed `iwms-government-frontend/src/layouts/admin/components/AppSidebar.tsx` so it
respects exactly the 5 ported forms (Trip Plans, Daily Trip Plan, Bin Collection Event, Waste
Collected Data, Collection Points). The earlier port added extra sidebar items I don't want.
- Do NOT restructure the sidebar or add new sections/entries.
- Only change allowed: remove the **Trip Plan Collection Point** entry (per Part 2) if it is
  still there.
- Register/keep routes consistent with what the fixed sidebar exposes.

## Requirements
- Corporation-based scoping everywhere (from the Config form) — no company/project fields or filters.
- Match the government codebase's conventions: encrypted routing, API hooks, form/table components,
  TypeScript types, i18n/locales.
- Run backend checks and migrations cleanly; confirm the frontend builds.
- Verify end-to-end: create a Trip Plan with multiple stops, edit it (stops reload and re-save
  correctly), delete it, and confirm the removed standalone form is gone from routes and sidebar.

dont ask me, i'll give you full access — work through it and fix anything related you find along the way.
